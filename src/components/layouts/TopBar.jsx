import {Layout} from 'antd';

const {Header} = Layout;

const TopBar = () => {
  return (
    <Header
          className="site-layout-background"
          style={{
            padding: 0,
          }}
        >
    </Header>
  )
}

export default TopBar;